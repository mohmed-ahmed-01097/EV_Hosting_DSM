# Survey Data

This folder contains one Phase 0-ready survey workbook used by `data_loader(cfg)`:

```text
Household_Energy_Survey.xlsx
```

This workbook contains all required Phase 0 sheets and columns. No separate legacy copy is included in this package.

See `docs/SURVEY_PHASE0_MAPPING.md` for the normalization and validation details.
